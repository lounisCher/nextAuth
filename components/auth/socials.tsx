'use client'

import { Button } from "../ui/button"

export default function Socials(){
    return(
        <div>
            <Button
            variant={"outline"}>Sign in with GOOGLE</Button>
            <Button
            variant={"outline"}>Sign in with GITHUB</Button>
        </div>
    )
}